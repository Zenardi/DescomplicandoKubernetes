apiVersion: v1
kind: Service
metadata:
  name: web
  namespace: tipsbank-web
  labels:
    app: web
    {{- include "tipsbank.labels" . | nindent 4 }}
spec:
  selector:
    app: web
  ports:
    - name: http
      port: 8080
      targetPort: 8080
